#ifndef LORD__UTILS_CPP
#define LORD__UTILS_CPP

#include <vector>

using namespace std;

typedef vector<int> List;
typedef pair<int, int> Pair;

Pair get_last_action(List&);
//int* unpack_state(int*, int*);

#endif